# application-gateway-service

- **Layer:** Application Gateway (Layer 4)
- **Target shape reached in:** R6
- **Reference page:** [application-gateway-service — Services Reference](https://oraclous.atlassian.net/wiki/spaces/OP/pages/131124) *(read-only Confluence mirror)*

Consult the reference page before changing this service. The root `CLAUDE.md` governs everything else; this file only carries the per-service pointer.
