# capability-registry-service

- **Layer:** Capability Registry (Layer 2)
- **Target shape reached in:** R2
- **Reference page:** [capability-registry-service — Services Reference](https://oraclous.atlassian.net/wiki/spaces/OP/pages/884757) *(read-only Confluence mirror)*

Consult the reference page before changing this service. The root `CLAUDE.md` governs everything else; this file only carries the per-service pointer.
