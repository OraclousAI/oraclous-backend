# harness-runtime-service

- **Layer:** Harness Runtime (Layer 3)
- **Target shape reached in:** R4
- **Reference page:** [harness-runtime-service — Services Reference](https://oraclous.atlassian.net/wiki/spaces/OP/pages/688350) *(read-only Confluence mirror)*

- **Tool dispatch rule (#956):** the runtime binds the operation, never the model — see `README.md` § "Tool dispatch" before touching `harness_execution_service`'s dispatch or `domain/tool_schemas.py`.

Consult the reference page before changing this service. The root `CLAUDE.md` governs everything else; this file only carries the per-service pointer.
