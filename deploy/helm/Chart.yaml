apiVersion: v2
name: oraclous-backend
description: Production Helm chart for the Oraclous Platform backend — all nine services + their workers, migrate/seed hook Jobs, the gateway Ingress, and Neo4j role provisioning. Encodes the prod security contract (RLS DSN split, RUN_MODE=prod, secrets-as-secretKeyRef). Substrate (Postgres/Neo4j/Redis) is operator-managed/external.
type: application
version: 0.1.0
appVersion: "0.1.0"
maintainers:
  - name: Oraclous Platform
home: https://github.com/oraclous
