[project]
name = "oraclous-errors"
version = "0.0.0"
description = "Shared error taxonomy and error-envelope emitter (shared)."
requires-python = "==3.12.*"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/oraclous_errors"]
